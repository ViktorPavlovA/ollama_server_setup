# Add parameters in .env_model

```
MODEL_PATH=/models/YOUR_MODEL_HERE.gguf
HOST=0.0.0.0
PORT=8282 #fix value, if you want change, you need also change dockerfile_model
CTX_SIZE=16384
GPU_LAYERS=40
FLASH_ATTN=off

```